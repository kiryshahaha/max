# GUAP Student Portal API

FastAPI бэкенд для студенческого портала ГУАП с интеграцией данных через парсер ЛК.

## 🚀 Основные возможности

- Получение данных студента из ЛК ГУАП
- REST API для фронтенд приложений
- Интеграция с Supabase для хранения данных
- Поддержка расписания, задач, оценок, отчетов, дедлайнов

## 📁 Структура backend

```
backend/app/
├── main.py                 # Точка входа FastAPI приложения
├── db/
│   ├── supabase_client.py  # Клиент для работы с Supabase
│   ├── dependencies.py     # Зависимости FastAPI
│   └── models.py          # Модели данных Pydantic
├── routes/
│   ├── tasks.py           # Эндпоинты задач
│   ├── schedule.py        # Эндпоинты расписания
│   ├── profile.py         # Эндпоинты профиля
│   ├── marks.py           # Эндпоинты оценок
│   ├── reports.py         # Эндпоинты отчетов
│   ├── users.py           # Эндпоинты пользователей
│   ├── faculties.py       # Эндпоинты факультетов
│   ├── teachers.py        # Эндпоинты преподавателей
│   └── rooms.py           # Эндпоинты аудиторий
├── requirements.txt       # Зависимости Python
└── README.md             # Документация
```

## 🛠️ Установка и запуск

### 1. Настройка виртуального окружения

```bash
cd backend/app
python -m venv myenv

# Linux/Mac
source myenv/bin/activate

# Windows
myenv\Scripts\activate
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

Создайте файл `.env` в папке `backend/`:

```env
SUPABASE_URL=your_supabase_url
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
```

### 4. Запуск сервера

```bash
uvicorn main:app --reload --port 8000
```

Сервер будет доступен по адресу: `http://localhost:8000`

## 📚 Документация API

После запуска сервера доступна автоматическая документация:
- Swagger UI: `http://localhost:8000/docs`
- ReDoc: `http://localhost:8000/redoc`

## 🔌 Основные эндпоинты

### 📋 Задачи

- `GET /tasks?uid={user_id}` - Получить список задач пользователя

**Пример ответа:**

```json
{
  "success": true,
  "tasks": [
    {
      "task": {
        "id": 168453,
        "name": "ЛАБОРАТОРНАЯ РАБОТА №1",
        "type": "Лабораторная работа",
        "deadline": "Спи спокойно"
      },
      "score": {
        "max": 0,
        "achieved": 0
      },
      "status": {
        "code": "accepted",
        "text": "принят"
      }
    }
  ],
  "tasks_count": 32,
  "uid": "user_id"
}
```

### 📅 Расписание

- `GET /schedule?uid={user_id}` - Расписание на сегодня, вчера и завтра
- `GET /schedule/today?uid={user_id}` - Расписание на сегодня
- `GET /schedule/tomorrow?uid={user_id}` - Расписание на завтра  
- `GET /schedule/yesterday?uid={user_id}` - Расписание на вчера
- `GET /schedule/week?uid={user_id}` - Недельное расписание

**Пример ответа:**

```json
{
  "success": true,
  "uid": "user_id",
  "today": {
    "date": "2025-11-14",
    "date_dd_mm": "14.11",
    "day_name": "Пт",
    "schedule": [
      {
        "type": "Л",
        "subject": "Теория вероятностей",
        "timeRange": "09:30 - 11:00",
        "location": "32-01"
      }
    ]
  }
}
```

### 👤 Профиль

- `GET /profile?uid={user_id}` - Получить профиль пользователя

**Пример ответа:**

```json
{
  "success": true,
  "profile": {
    "personal_info": {
      "full_name": "Иванов Иван Иванович",
      "student_id": "2024/4034"
    },
    "academic_info": {
      "group": "4433",
      "education_form": "очная"
    }
  }
}
```

### 📊 Оценки

- `GET /marks?uid={user_id}` - Получить оценки пользователя

**Пример ответа:**

```json
{
  "success": true,
  "marks": [
    {
      "subject": {
        "name": "Алгоритмы и структуры данных"
      },
      "control": {
        "typeText": "Экзамен",
        "status": "pending"
      },
      "credits": {
        "value": 4
      }
    }
  ]
}
```

### 📄 Отчеты

- `GET /reports?uid={user_id}` - Получить отчеты пользователя

**Пример ответа:**

```json
{
  "success": true,
  "reports": [
    {
      "task": {
        "name": "ЛАБОРАТОРНАЯ РАБОТА №1"
      },
      "status": {
        "text": "принят"
      },
      "load_date": {
        "text": "06.11.2025 17:10:16"
      }
    }
  ]
}
```

## 🏫 Дополнительные эндпоинты

### Факультеты

- `GET /faculties` - Список факультетов
- `GET /faculties/{id}` - Информация о факультете

### Преподаватели

- `GET /teachers` - Список преподавателей  
- `GET /teachers/{id}` - Информация о преподавателе

### Аудитории

- `GET /rooms` - Список аудиторий

### Пользователи (админка)

- `GET /users` - Список пользователей
- `GET /users/{id}` - Информация о пользователе

## 🗃️ Модель данных

### Структура хранения в Supabase

Данные в системе организованы следующим образом:

**users** (Authentication) - Основная информация о пользователях

**user_data** - Расширенные данные пользователей:
- `profile` - профиль студента
- `tasks` - список задач
- `week_schedule` - расписание на неделю
- `today_schedule` - расписание на сегодня
- `marks` - оценки
- `reports` - отчеты
- `materials` - учебные материалы

## 🔧 Технологии

Проект построен на следующих технологиях:

- **FastAPI** - современный Python фреймворк для создания REST API
- **Supabase** - база данных и аутентификация на основе PostgreSQL
- **Pydantic** - валидация и сериализация данных
- **Uvicorn** - ASGI сервер
- **Python 3.8+** - язык программирования

## 📝 Логирование

Приложение использует подробное логирование для отладки и мониторинга:
- Поиск пользователей по UID
- Структура найденных данных
- Ошибки подключения к БД
- Информация о выполнении запросов


